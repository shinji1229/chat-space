$(function(){
  $('#group_name').on('keyup',function(e){
    e.preventDefault();
    var input = $('#group_name').val();
  });

  $('#group_name').on('keyup',function(e){
    e.preventDefault();
    var input = $('#group_name').val();
  });
});