.chat-side
 .chat-side__user
  .chat-side__user_name
   = current_user.name
  = link_to edit_user_registration_path do
   %i.fa.fa-cog.fa-fw
  = link_to new_group_path, 'data-turbolinks': false do
   %i.fa.fa-pencil-square-o.fa-fw

 .chat-side__groups
  = render @groups
